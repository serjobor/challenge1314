# Челлендж. 1.3. 1.4

## Предоставьте решение в виде содержимого yml-файла actions.yml, который бы позволял развертывать собранный статический сайт на сервисе play-with-docker. 

Было предложено на занятии 2 варианта: 

- с hub.docker.com,
- и без него (напрямую скопировать содержимое на play-with-docker инстанс и запустить с помощью docker-compose up, скопированный файл docker-compose.yml).
  
В качестве ответа ссылка на репозиторий с экшеном или сам файл экшена (yml).